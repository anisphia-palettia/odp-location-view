export interface CoordinateItem {
    id: number
    imageName: string
    long: string
    lat: string
    groupId: number
    address: string
    urlId: string
    isAccepted: boolean
    isReject: boolean
    photoTakenAt: string
    createdAt: string
    updatedAt: string
}
